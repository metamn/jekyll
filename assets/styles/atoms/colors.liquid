---
---

{% for page in site.pages %}{% if page.title == "Colors" %}{% for color in page.items %}${{ color.name }}: {{ color.value }};
{% endfor %}{% endif %}{% endfor %}{% capture colors %}{% for page in site.pages %}{% if page.title == "Colors" %}{% for color in page.items %}{{ color.value }},{% endfor %}{% endif %}{% endfor %}{% endcapture %}{% capture colors %}{% include styleguide/atoms/sg-clean-spaces.html text=colors %}{% endcapture %}{% assign colors = colors | split: ',' %}
$colors: {{ colors | join: ', ' }};


@mixin colors($scheme: black-on-white) {
  {% for page in site.pages %}{% if page.title == "Colors" %}{% for pair in page.pairs %}
    @if ($scheme == {% include site/atoms/classname.html title=pair.name %}) {
      color: ${{ pair.foreground }};
      background-color: ${{ pair.background }};
    }
  {% endfor %}{% endif %}{% endfor %}
}


{% capture pairs %}{% for page in site.pages %}{% if page.title == "Colors" %}{% for pair in page.pairs %}{% include site/atoms/classname.html title=pair.name %},{% endfor %}{% endif %}{% endfor %}{% endcapture %}{% assign pairs = pairs | split: ',' %}
$color-pairs: {{ pairs | join: ', ' }};